// import { selectPageExerciseDomain } from '../selectors';

describe('selectPageExerciseDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
